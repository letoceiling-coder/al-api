# 📊 Отчет о выполнении команды deploy:trendagent

**Дата:** $(date)  
**Команда:** `php artisan deploy:trendagent`

---

## 🔍 Детальный отчет

См. вывод команды ниже.

---

## ✅ Что делает команда

1. **Определяет окружение** (локальное/серверное)
2. **Локально:** Отправляет изменения в Git
3. **На сервере:**
   - Обновляет проект из Git
   - Устанавливает зависимости Composer
   - Устанавливает зависимости NPM
   - Собирает проект (npm run build)
   - Добавляет версию к assets (для обхода кеша)
   - Выполняет миграции
   - Очищает кеш Laravel

---

## 📝 Примечания

- Команда автоматически определяет, где она запущена
- Все шаги логируются в отчет
- При ошибках команда продолжает работу и выводит детальный отчет
