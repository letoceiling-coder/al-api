# Анализ занятости диска на сервере 89.169.39.244

**Дата проверки:** 2025  
**Корневой раздел:** `/dev/vda1` — **29G занято из 29G (100%)**

---

## Главные потребители

| Путь | Размер | Причина |
|------|--------|--------|
| **/var/lib/mysql** | **~16 ГБ** | Данные MySQL + **бинарные логи ~9.8 ГБ** (126 файлов × ~100 МБ) |
| /var/www | 4 ГБ | Проекты (AL, messager, auto.siteaccess.ru и др.) |
| /var/log | 1.6 ГБ | systemd journal ~1.1 ГБ, syslog ~280 МБ |
| /root/.npm | 1.7 ГБ | Кэш npm |
| /root | 2.3 ГБ | .npm, .cache, .cursor-server, bot |
| /usr | 3.8 ГБ | Системные пакеты |

---

## Почему диск заполнен

1. **MySQL binary logs** — включено логирование репликации/восстановления; старые файлы не удаляются и занимают **~10 ГБ**. Это основная причина.
2. **systemd journal** — хранит много старых логов (~1.1 ГБ).
3. **syslog** — один большой файл ~280 МБ.
4. **Кэш npm** в `/root/.npm` — ~1.7 ГБ, можно безопасно чистить.

---

## Рекомендуемые действия (по приоритету)

### 1. Очистить старые бинарные логи MySQL (освободит ~9–10 ГБ)

На сервере:

```bash
# Посмотреть текущие бинарные логи
mysql -e "SHOW BINARY LOGS;"

# Удалить все кроме последних 3 файлов (или оставить последние 2 дня)
mysql -e "PURGE BINARY LOGS BEFORE DATE(NOW() - INTERVAL 2 DAY);"
# или оставить только последние 3 файла:
# mysql -e "SHOW BINARY LOGS;"  # запомнить имя 3-го с конца, затем:
# mysql -e "PURGE BINARY LOGS TO 'binlog.000122';"  # пример — подставить свой файл
```

После очистки при желании ограничить размер логов в конфиге MySQL (например в `/etc/mysql/mysql.conf.d/mysqld.cnf`):

```ini
expire_logs_days = 2
# или в новых версиях:
# binlog_expire_logs_seconds = 172800
```

### 2. Ограничить размер systemd journal (~1 ГБ → ~100 МБ)

```bash
# Оставить только последние 3 дня
journalctl --vacuum-time=3d

# Или ограничить размер
journalctl --vacuum-size=100M
```

### 3. Очистить кэш npm в /root (~1.7 ГБ)

```bash
npm cache clean --force
# или
rm -rf /root/.npm/_cacache
```

### 4. Обрезать/ротировать syslog (по желанию)

```bash
# Обрезать текущий syslog (логи не потеряются, если есть logrotate)
truncate -s 0 /var/log/syslog
```

---

## Скрипт быстрой безопасной очистки

См. файл `server_disk_cleanup.sh` — запускать на сервере после просмотра команд выше.

---

## Итог

| Действие | Примерно освободится |
|----------|----------------------|
| PURGE BINARY LOGS (MySQL) | **~9–10 ГБ** |
| journalctl --vacuum-time=3d | **~1 ГБ** |
| npm cache clean | **~1.7 ГБ** |
| **Итого** | **~12 ГБ** |

После этого можно снова развернуть конфиги Nginx для auto.siteaccess.ru и p-d-a-b.neeklo.ru.
